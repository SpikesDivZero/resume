\companyName{Squarespace}
\companyLocation{Remote}
\companyJobs{
  % TODO: Is there a better way? Essentially, we want to call this after the company header is printed However, I still
  % want to keep company name/location clearly defined (I previously used a single command to set the header, but I
  % didn't like that the args were not named....)
  \includeJob{2017-sr-swe}
}
